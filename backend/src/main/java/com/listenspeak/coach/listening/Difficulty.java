package com.listenspeak.coach.listening;

/**
 * Target style complexity for generated exercises.
 *
 * <p>These are authoring guides for the generator, not official calibrated CELPIP
 * levels, and the UI says so.
 */
public enum Difficulty {
    /** Roughly CELPIP 5-7 style complexity. */
    DEVELOPING,
    /** Roughly CELPIP 8-10 style complexity. */
    COMPETENT,
    /** Roughly CELPIP 10-12 style complexity. */
    ADVANCED
}
